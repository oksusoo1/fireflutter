import 'package:flutter/material.dart';

class ForumListScreen extends StatelessWidget {
  const ForumListScreen({required this.arguments, Key? key}) : super(key: key);

  static const String routeName = '/forumListScreen';

  final Map arguments;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
